version: '3.8'

services:
  venom-tempmail:
    build: .
    container_name: venom-tempmail-bot
    restart: always
    ports:
      - "3000:3000"
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - PORT=3000
      - POLL_INTERVAL_MS=4000
    volumes:
      - ./data:/app/data
